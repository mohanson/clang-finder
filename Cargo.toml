[package]
name = "clang-finder"
version = "0.1.0"
edition = "2024"
description = "Locate an available clang executable on the system, primarily for use in build.rs scripts."
license = "MIT"

[dependencies]
